#include "../include/gui.h"
#include "../include/tcpclient.h"

int main() {
    TCPClient tcp;
    //GUI gui;
    //std::thread netThread(netThreadFunc, gui.netclient);
    //gui.init();
    return 0;
}
